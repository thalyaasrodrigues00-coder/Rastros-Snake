import 'package:flutter/material.dart';

class BannerAdFooter extends StatelessWidget {
  const BannerAdFooter({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
